# 2026-08-17

## `dbe6493` fix. npm audit 취약점 수정 - nanoid/postcss 의존성 업데이트로 취약점 2건(high 1, moderate 1) 해결 - 프론트엔드 테스트/빌드 재검증

- 수정 파일: `frontend/package-lock.json`

### 목적 및 이유
- **23단계(npm audit 취약점 확인/수정)** 진행: 22단계(프론트엔드 테스트 도입) 때 `npm install` 과정에서 `npm audit` 결과 취약점 2건(high 1, moderate 1)이 있다는 걸 확인했는데, 그때는 손대지 않고 넘어갔던 부분을 이어서 처리
- `npm ls`로 확인한 결과, 취약점의 실제 원인은 `nanoid`(high — 크기 0으로 커스텀 생성기를 만들면 무한루프 가능), `postcss`(moderate — `from` 옵션 미설정 시 공격자가 지정한 소스맵 파일을 읽을 수 있음) 두 패키지였고, 둘 다 `vite → postcss → nanoid` 경로로 들어오는 **Vite의 빌드타임 의존성**이라는 걸 확인 — 즉 배포된 프로덕션 번들(브라우저에서 실행되는 코드)에는 포함되지 않고, 로컬 개발 서버·CI 빌드 과정에서만 쓰이는 도구였음
- `npm audit fix` 실행 — breaking change 없는 semver 호환 버전으로 자동 패치되어 48개 패키지가 업데이트되고 취약점이 0건으로 정리됨 (`--force` 없이 해결 가능한 안전한 수준이었음)
- 검증: 패치 후 `npm run test`(7개 테스트 전부 통과)와 `npm run build`(정상 빌드)를 재실행해서 의존성 업데이트로 인한 회귀가 없는지 확인
