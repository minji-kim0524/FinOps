# 2026-07-30

## `29fd6e6` feat. 계산 이력 수정/삭제 기능 추가 - PUT/DELETE /records 엔드포인트 구현 - 계산 필드 적용 로직 공통 헬퍼로 리팩터링 - 프론트엔드에 수정 모달/삭제 확인 UI 추가 - pytest 테스트 보강

- 수정 파일: `backend/app/main.py`, `backend/tests/test_main.py`, `frontend/src/App.jsx`

### 목적 및 이유
- **9단계(이력 수정/삭제 기능)** 진행: 6단계에서 CSV 일괄 업로드 기능을 만들 때 범위에서 제외했던 부분을 이어서 구현
- `main.py`: `PUT /records/{id}`(값을 바꾸면 4대보험·소득세 등을 전부 재계산해서 저장), `DELETE /records/{id}` 엔드포인트 추가. 존재하지 않는 id 요청은 404로 처리(`_get_record_or_404` 헬퍼)
- 기존 `_save_calculated`에 있던 "계산 결과를 SalaryRecord 필드에 채워 넣는" 로직을 `_apply_calculated_fields` 헬퍼로 분리해, 신규 생성과 수정(재계산) 양쪽에서 재사용 — 중복 제거
- `frontend/App.jsx`: 이력 표에 "관리" 컬럼 추가
  - 수정: 클릭 시 모달이 뜨고 기존 값(직원명/세전급여/부양가족 수)이 그대로 채워짐. 저장하면 서버가 재계산한 값으로 표가 갱신
  - 삭제: Ant Design `Popconfirm`으로 "삭제하시겠습니까?" 확인 후 삭제
- 검증: pytest 4개 추가(수정 시 재계산 검증, 수정/삭제 각각의 404 케이스) → 총 12개 통과. 브라우저에서 직접 홍길동 이력을 세전 300만원 → 500만원으로 수정해 모든 공제액이 재계산되는 것을 확인하고, 이어서 삭제까지 실제로 수행해 표에서 사라지는 것을 확인. 콘솔 에러 없음
